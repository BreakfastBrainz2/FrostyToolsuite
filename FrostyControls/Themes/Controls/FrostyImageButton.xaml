<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrl="clr-namespace:Frosty.Controls">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/FrostyControls;component/Themes/Brushes.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- FrostyImageButton -->
  <Style TargetType="{x:Type ctrl:FrostyImageButton}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ctrl:FrostyImageButton}">
          <Grid Height="88"
                Width="88">
            <Button x:Name="PART_Button"
                    Cursor="Hand">
              <Button.Style>
                <Style TargetType="{x:Type Button}">
                  <Setter Property="Foreground"
                          Value="{StaticResource FontColor}" />
                  <Setter Property="Background"
                          Value="{StaticResource ControlBackground}" />
                  <Setter Property="Padding"
                          Value="2" />
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="buttonBorder"
                                Background="Transparent"
                                BorderThickness="0"
                                CornerRadius="4">
                          <Grid x:Name="g1"
                                Height="88"
                                Width="88"
                                Background="{StaticResource ControlBackground}">
                            <Border Background="{StaticResource ListBackground}"
                                    Width="80"
                                    Height="80" />
                            <Border Background="{StaticResource ListBackground}"
                                    Width="88"
                                    Height="60" />
                            <Border Background="{StaticResource ListBackground}"
                                    Width="60"
                                    Height="88" />
                            <StackPanel VerticalAlignment="Center">
                              <Label
                                Content="{Binding Path=Title, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyImageButton}}}"
                                ContentStringFormat="NO {0}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                FontFamily="Global User Interface"
                                FontWeight="Bold"
                                Opacity="0.5" />
                              <Label Content="Click to import"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"
                                     FontFamily="Global User Interface"
                                     FontSize="10"
                                     Opacity="0.5"
                                     Margin="0,-10,0,0" />
                            </StackPanel>
                          </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                          <Trigger Property="IsMouseOver"
                                   Value="True">
                            <Setter TargetName="g1"
                                    Property="Background"
                                    Value="#FF707070" />
                          </Trigger>
                          <Trigger Property="IsPressed"
                                   Value="True">
                            <Setter TargetName="g1"
                                    Property="Background"
                                    Value="#FF585858" />
                          </Trigger>
                        </ControlTemplate.Triggers>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                </Style>
              </Button.Style>
            </Button>
            <Image x:Name="PART_Image"
                   IsHitTestVisible="False" />
            <Button x:Name="PART_ClearButton"
                    Width="16"
                    Height="16"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,1,1,0"
                    Visibility="Collapsed">
              <Image Source="/FrostyControls;component/Images/Clear.png" />
            </Button>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>