<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctrl="clr-namespace:Frosty.Controls">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/FrostyControls;component/Themes/Brushes.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <!-- FrostyMessageBox -->
  <Style TargetType="{x:Type ctrl:FrostyMessageBox}">
    <Setter Property="ContentTemplate">
      <Setter.Value>
        <DataTemplate>
          <DataTemplate.Resources>
            <ctrl:MessageBoxClickCommand x:Key="messageBoxClickCommand" />
          </DataTemplate.Resources>
          <Grid Background="{StaticResource WindowBackground}">
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="38" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource ListBackground}">
              <TextBlock x:Name="messageBoxText"
                         Margin="12,24"
                         TextWrapping="Wrap"
                         Text="{Binding Text, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyMessageBox}}}"
                         Foreground="#F8F8F8"
                         TextAlignment="{Binding Path=Alignment, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyMessageBox}}}"
                         VerticalAlignment="Center" />
            </Border>

            <Border Grid.Row="1">
              <Grid>
                <Button x:Name="PART_MessageBoxOk"
                        Content="OK"
                        Margin="8"
                        HorizontalAlignment="Right"
                        Grid.Row="1"
                        Width="75"
                        Command="{StaticResource messageBoxClickCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}" />

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal">
                  <Button x:Name="PART_MessageBoxCancel"
                          Content="Cancel"
                          HorizontalAlignment="Left"
                          Margin="8,8,0,8"
                          Width="75"
                          Visibility="Collapsed"
                          Command="{StaticResource messageBoxClickCommand}"
                          CommandParameter="{Binding RelativeSource={RelativeSource Self}}" />
                  <Button x:Name="PART_MessageBoxNo"
                          Content="No"
                          HorizontalAlignment="Left"
                          Margin="8"
                          Width="75"
                          Visibility="Collapsed"
                          Command="{StaticResource messageBoxClickCommand}"
                          CommandParameter="{Binding RelativeSource={RelativeSource Self}}" />
                </StackPanel>

                <Button x:Name="PART_MessageBoxYes"
                        Content="Yes"
                        HorizontalAlignment="Right"
                        Margin="8"
                        Grid.Row="1"
                        Width="75"
                        Visibility="Collapsed"
                        Command="{StaticResource messageBoxClickCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}" />
              </Grid>
            </Border>
          </Grid>

          <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=Buttons, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyMessageBox}}}"
                         Value="OKCancel">
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxCancel"
                      Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=Buttons, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyMessageBox}}}"
                         Value="YesNoCancel">
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxYes"
                      Value="Visible" />
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxNo"
                      Value="Visible" />
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxCancel"
                      Value="Visible" />
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxOk"
                      Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=Buttons, RelativeSource={RelativeSource AncestorType={x:Type ctrl:FrostyMessageBox}}}"
                         Value="YesNo">
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxYes"
                      Value="Visible" />
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxNo"
                      Value="Visible" />
              <Setter Property="Visibility"
                      TargetName="PART_MessageBoxOk"
                      Value="Collapsed" />
            </DataTrigger>
          </DataTemplate.Triggers>
        </DataTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>