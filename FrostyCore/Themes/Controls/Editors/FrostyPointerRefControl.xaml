<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:editors="clr-namespace:Frosty.Core.Controls.Editors"
                    xmlns:converters="clr-namespace:Frosty.Core.Converters"
                    xmlns:controls="clr-namespace:Frosty.Controls;assembly=FrostyControls">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/FrostyControls;component/Themes/Brushes.xaml" />
    <ResourceDictionary Source="/FrostyControls;component/Themes/Icons.xaml" />
    <ResourceDictionary Source="/FrostyCore;component/Themes/Icons.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <Style TargetType="{x:Type editors:FrostyPointerRefControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type editors:FrostyPointerRefControl}">
          <ControlTemplate.Resources>
            <converters:StringToBitmapSourceConverter x:Key="stringToBitmapConverter" />
            <editors:ClassTypeToStringConverter x:Key="classTypeToStringConverter" />
            <converters:AssetEntryToBitmapSourceConverter x:Key="AssetEntryToBitmapSourceConverter" />
            <editors:RefTypeToStringConverter x:Key="RefTypeToStringConverter" />
            <Style x:Key="MenuButtonStyle"
                   TargetType="{x:Type Button}">
              <Setter Property="Foreground"
                      Value="{StaticResource FontColor}" />
              <Setter Property="Background"
                      Value="Transparent" />
              <Setter Property="Padding"
                      Value="2" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="buttonBorder"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="0">
                      <ContentPresenter x:Name="content"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver"
                               Value="True">
                        <Setter TargetName="buttonBorder"
                                Property="Background"
                                Value="#FF707070" />
                      </Trigger>
                      <Trigger Property="IsPressed"
                               Value="True">
                        <Setter TargetName="buttonBorder"
                                Property="Background"
                                Value="#FF585858" />
                      </Trigger>
                      <Trigger Property="IsEnabled"
                               Value="False">
                        <Setter TargetName="content"
                                Property="Opacity"
                                Value="0.5" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ControlTemplate.Resources>

          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition x:Name="buttonColumn"
                                Width="19" />
            </Grid.ColumnDefinitions>

            <ComboBox x:Name="PART_Popup"
                      Grid.Column="0"
                      Grid.ColumnSpan="2"
                      VirtualizingPanel.VirtualizationMode="Recycling">
              <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ComboBox.ItemsPanel>

              <ComboBox.ItemTemplate>
                <DataTemplate>
                  <Grid Height="18">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition MinWidth="260" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="{Binding Path=., Converter={StaticResource classTypeToStringConverter}, ConverterParameter=Name}"
                               Foreground="{StaticResource FontColor}"
                               VerticalAlignment="Center"
                               FontFamily="MS Reference Sans Serif"
                               TextTrimming="CharacterEllipsis" />
                    <TextBlock Grid.Column="1"
                               Text="{Binding Path=., Converter={StaticResource classTypeToStringConverter}, ConverterParameter=Guid}"
                               Foreground="{StaticResource FontColor}"
                               VerticalAlignment="Center"
                               FontFamily="Consolas"
                               HorizontalAlignment="Right" />
                  </Grid>
                </DataTemplate>
              </ComboBox.ItemTemplate>

              <ComboBox.ItemContainerStyle>
                <Style TargetType="{x:Type ComboBoxItem}">
                  <Setter Property="HorizontalContentAlignment"
                          Value="Stretch" />
                  <Style.Triggers>
                    <Trigger Property="IsSelected"
                             Value="True">
                      <Setter Property="Background"
                              Value="{StaticResource SubItemHighlight}" />
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </ComboBox.ItemContainerStyle>

              <ComboBox.Template>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                  <Grid>
                    <Popup x:Name="PART_PopupMenu"
                           AllowsTransparency="true"
                           Grid.ColumnSpan="2"
                           IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                           Margin="1"
                           PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                           Placement="Bottom"
                           Focusable="False">
                      <Border x:Name="dropDownBorder"
                              Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                              BorderBrush="{StaticResource ControlBackground}"
                              BorderThickness="1">
                        <Grid>
                          <StackPanel Grid.Row="0"
                                      Orientation="Vertical"
                                      MaxHeight="330"
                                      Background="{StaticResource ListBackground}">
                            <Button x:Name="PART_ClearButton"
                                    Content="Clear assigned object"
                                    Style="{StaticResource MenuButtonStyle}"
                                    FontFamily="MS Reference Sans Serif"
                                    Height="22" />
                            <Button x:Name="PART_OpenButton"
                                    Content="Open asset"
                                    Style="{StaticResource MenuButtonStyle}"
                                    FontFamily="MS Reference Sans Serif"
                                    Height="22" />
                            <Button x:Name="PART_FindButton"
                                    Content="Find in data explorer"
                                    Style="{StaticResource MenuButtonStyle}"
                                    FontFamily="MS Reference Sans Serif"
                                    Height="22" />
                            <Button x:Name="PART_CreateButton"
                                    Content="Create new ..."
                                    Style="{StaticResource MenuButtonStyle}"
                                    FontFamily="MS Reference Sans Serif"
                                    Height="22" />

                            <Border x:Name="PART_Separator"
                                    Height="22">
                              <Separator Margin="1,0,1,0" />
                            </Border>

                            <controls:FrostyWatermarkTextBox x:Name="PART_FilterTextBox"
                                                         Height="24"
                                                         Margin="1,0,1,1"
                                                         WatermarkText="Filter"
                                                         VerticalContentAlignment="Center"
                                                         BorderThickness="1" />

                            <Border x:Name="PART_TBBorder"
                                    Height="22">
                              <TextBlock x:Name="PART_TextBlock"
                                         VerticalAlignment="Center"
                                         Text="Select Class"
                                         Margin="4,0,0,0"
                                         Foreground="{StaticResource FontColor}"
                                         Opacity="0.5"
                                         FontFamily="MS Reference Sans Serif" />
                            </Border>

                            <ScrollViewer x:Name="DropDownScrollViewer"
                                          MaxHeight="198">
                              <Grid x:Name="grid"
                                    RenderOptions.ClearTypeHint="Enabled"
                                    HorizontalAlignment="Stretch">
                                <ItemsPresenter x:Name="ItemsPresenter"
                                                KeyboardNavigation.DirectionalNavigation="Contained"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                              </Grid>
                            </ScrollViewer>
                          </StackPanel>
                        </Grid>
                      </Border>
                    </Popup>
                  </Grid>
                </ControlTemplate>
              </ComboBox.Template>
            </ComboBox>

            <Grid Grid.Column="0"
                  x:Name="refStackPanel">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>

              <Button Grid.Column="0"
                      x:Name="PART_AssignButton"
                      Width="12"
                      Height="12"
                      Padding="0"
                      VerticalAlignment="Center"
                      Background="Transparent"
                      BorderBrush="Transparent"
                      BorderThickness="0"
                      HorizontalAlignment="Left"
                      Margin="0 0 4 0"
                      ToolTip="Assign from selected asset"
                      IsEnabled="{Binding Path=IsEnabled, RelativeSource={RelativeSource TemplatedParent}}">
                <Button.Content>
                  <Path Margin="0 0 0 0"
                        Width="10"
                        Height="10"
                        Fill="#f7f7f7"
                        Stretch="Uniform"
                        Data="{Binding Path=Data, Source={StaticResource AssignIcon}}"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center" />
                </Button.Content>
              </Button>

              <Image x:Name="PART_TypeImage"
                     Grid.Column="1"
                     Width="12"
                     Height="12"
                     Margin="0,0,4,0"
                     ToolTip="{Binding Path=RefType, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource RefTypeToStringConverter}}"
                     Source="{Binding Path=RefType, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource AssetEntryToBitmapSourceConverter}}" />

              <TextBlock x:Name="PART_RefName"
                         Grid.Column="2"
                         Text="{Binding Path=RefValueName, RelativeSource={RelativeSource TemplatedParent}}"
                         ToolTip="{Binding Path=RefTooltip, RelativeSource={RelativeSource TemplatedParent}}"
                         VerticalAlignment="Center"
                         TextWrapping="NoWrap"
                         TextTrimming="CharacterEllipsis" />
              <TextBlock Grid.Column="3"
                         x:Name="refValuePath"
                         VerticalAlignment="Center"
                         TextWrapping="NoWrap"
                         TextTrimming="CharacterEllipsis"
                         Margin="4 0 0 0"
                         Opacity="0.5">
                <TextBlock.Text>
                  <Binding Path="RefValuePath"
                           RelativeSource="{RelativeSource TemplatedParent}"
                           StringFormat="({0})" />
                </TextBlock.Text>
              </TextBlock>
            </Grid>

            <Button x:Name="PART_MoreOptionsButton"
                    Grid.Column="1"
                    Margin="1"
                    ToolTip="Show more options"
                    Width="17"
                    IsEnabled="{Binding Path=IsEnabled, RelativeSource={RelativeSource TemplatedParent}}">
              <Button.Content>
                <Image Source="/FrostyEditor;component/Images/More.png"
                       Width="14" />
              </Button.Content>
            </Button>

          </Grid>

          <ControlTemplate.Triggers>
            <Trigger Property="RefValuePath"
                     Value="">
              <Setter TargetName="refValuePath"
                      Property="Visibility"
                      Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsEnabled"
                     Value="False">
              <Setter TargetName="PART_TypeImage"
                      Property="Opacity"
                      Value="0.5" />
              <Setter TargetName="PART_RefName"
                      Property="Opacity"
                      Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>