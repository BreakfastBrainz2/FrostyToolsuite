<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:editors="clr-namespace:Frosty.Core.Controls.Editors">
  <Style TargetType="{x:Type editors:FrostyImagePathControl}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type editors:FrostyImagePathControl}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="19" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding Path=Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                       Grid.Column="0"
                       Foreground="{StaticResource FontColor}"
                       TextTrimming="CharacterEllipsis" />
            <Button x:Name="PART_BrowseButton"
                    Grid.Column="1"
                    Margin="1"
                    Width="17">
              <Button.Content>
                <Image Source="/FrostyEditor;component/Images/More.png"
                       Width="14" />
              </Button.Content>
            </Button>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>